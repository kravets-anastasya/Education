#include <iostream>
#include <algorithm>
using namespace std;
void gen(string str) {
    sort(str.begin(), str.end());
    do {
        cout << str << '\n';
    } while (next_permutation(str.begin(), str.end()));
}
 
int main() {
    string input;
    cin >> input;
    gen(input);
    return 0;
}
