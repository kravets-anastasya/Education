#include <iostream>
#include <string>
 
using namespace std;
 
int main() {
	string line{};
	getline(cin, line);
	int namber = line.size();
	int counter{};
	for (size_t i = 0; i < namber; i++) {
		if (line[i] == 'w' || line[i] == 'W') {
			counter++;
		}
	}
	cout << counter;
 
	return 0;
}
