/*
Игорь и Ира решили поехать на дачу, ведь там куча дел: прополоть и полить грядки, собрать урожай, окучить картошку, проверить сохранность забора и
много-много всего.

На огороде у Игоря и Иры растет множество разных культур — n штук. Пришло время уборки урожая. Игорь будет хранить урожай в одинаковых ящиках. 
Игорь посчитал, сколько получилось собрать килограмм каждой культуры - ai. Кроме того в один ящик помещается bi килограмм каждой культуры.

Игорь не хочет складывать в один ящик разные овощи, ведь так не получится их сохранить за зиму. Помогите Игорю посчитать, сколько потребуется ящиков, 
чтобы сохранить весь урожай.

Входные данные
В первой строке задано одно целое число n (0≤n≤10^5) — количество культур на огороде Игоря и Иры.

В каждой следующей строке записано два целых числа ai и bi (1≤ai,bi≤10^5) — количество урожая в килограммах i культуры и то, сколько килограммов 
можно хранить в одном ящике.

Выходные данные
Выведите одно целое число — количество ящиков, которое потребуется Игорю для хранения всего урожая.
*/


#include<iostream>
using namespace std;
int main()
{
    long long i = 0, n, a, b, k;
    cin >> n;
    long long sum = 0;
    for (i = 0; i < n; i++)
    {
        cin >> a >> b;
        if (a == 0 || b == 0) {
            continue;
        }
        k = a / b;
        if (a < b || a == b)
        {
            sum += 1;
        }
        if (a > b)
        {
            if (a % b == 0)
            {
                sum += k;
            }
            if (a % b != 0)
            {
                sum += (k + 1);
            }
        }
 
    }
    cout << sum << endl;
    return 0;
}
