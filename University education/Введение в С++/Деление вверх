/*
Вася пошёл в третий класс и только учится делить числа, но делает он это неправильно. Он хочет поделить целое число a на целое число b. 
Если a делится на b, то он получает верный результат. Если же при делении a на b имеется ненулевой остаток, то к результату деления он прибавляет единицу.

Напишите программу, которая по заданным a и b делит a на b так же, как это делает Вася.

Входные данные
Вводится 2 целых числа через пробел, сначала a затем b. (1≤a,b≤1000)

Выходные данные
Выведите единственное число – результат деления Васи.
*/


#include <iostream>
using namespace std;
 
int main() {
 
    size_t a{};
    size_t b{};
 
    cin >> a;
    cin >> b;
 
    size_t c = a / b;
 
    if (a%b != 0) {
        c++;
    }
 
    cout << c << endl;
 
    return 0;
}
