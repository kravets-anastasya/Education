/*
Последовательностью Фибоначчи называется последовательность, n-й член которой равен сумме предыдущих двух, т.е. Fn = Fn - 1 + Fn - 2, 
при этом F1 = 1, F2 = 1. Таким образом: F1 = 1, F2 = 1, F3 = F2 + F1 = 1 + 1 = 2, F4 = F3 + F2 = 2 + 1 = 3, F5 = F4 + F3 = 3 + 2 = 5 и так далее. 
По заданному n вычислить n-ое число Фибоначчи.

Входные данные
На ввод подается единственное число n

Выходные данные
Вывести n-ое число Фибоначчи (Fn)
*/


#include <iostream>
using namespace std;
 
int Fibom(int n, int sum) {
    int a1{ 1 };
    int a2{ 1 };
    int i{};
    while (i < n - 2) {
        sum = a1 + a2;
        a1 = a2;
        a2 = sum;
        i++;
    }
    return sum;
}
 
int main() {
    int n{};
    cin >> n;
    int sum{ 1 };
 
    cout << Fibom(n, sum) << endl;
    return 0;
}
