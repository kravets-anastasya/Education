/*
В этой задаче вам задан массив, требуется вывести его элементы в обратном порядке

Входные данные
В первой строке входных данных задано единственное целое число n (1 ≤ n ≤ 1000) – количество элементов массива.

Во второй строке записаны n чисел, не превосходящие 1000 – элементы массива

Выходные данные
В единственной строке выведите n чисел – заданный массив, идущий в обратном порядке.
*/



#include <iostream>
using namespace std;
int main() {
    int n{};
    int namber{ };
    int masiv[1000];
 
    cin >> n;
 
    for (size_t i = 0; i < n; i++) {
        cin >> masiv[i];
    }
    for (size_t i = n; i > 0;) {
        i--;
        cout << masiv[i] << " ";
 
    }
 
    return 0;
}
