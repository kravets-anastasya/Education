/*
Составить программу записи в массив B номеров четных элементов массива A.

Входные данные
В первой строке входных данных находится число N(1 ≤ N ≤ 1000) - число элементов в массиве. Во второй строке находятся N элементов.

Выходные данные
Выведите номера четных элементов массива.
*/


#include <iostream>
using namespace std;
int main() {
    int n{};
 
    int masiv[1000];
 
    cin >> n;
 
    for (size_t i = 0; i < n; i++) {
        cin >> masiv[i];
    }
    for (size_t i = 0; i < n; i++) {
         if ((masiv[i] % 2) == 0) {
             cout << i+1 << " ";
         }
    }
    
    
    return 0;
}
