/*
Найти номер последнего отрицательного элемента в массиве.

Входные данные
В первой строке входных данных находится число N(1 ≤ N ≤ 1000) - число элементов в массиве. Во второй строке находятся N элементов.

Выходные данные
Выведите одно целое число - номер последнего отрицательного элемента. Если его нет, то выведите 0.
*/


#include <iostream>
using namespace std;
int main() {
    int n{};
    int masiv[10000];
 
    cin >> n;
    for (size_t i = 0; i < n; i++)    {
        cin >> masiv[i];
    }
    int max = masiv[0];
    int index{};
    for (size_t i = 0; i < n; i++){
        if (masiv[i] < 0) { 
            max = masiv[i];
            index = i;
        }
    }
    cout << index + 1 << endl;
    
  
