/*
Однажды Влад, Довлет, Никита и Эдик проверяли письменные работы детей, и вот пришло время выставить оценки в рейтинг! 
Всего было N работ, и Владислав по порядку начал называть оценки Довлету чтобы он их выставил. Но вот какая проблема, 
Довлет не расслышал все оценки, а Влад повторять этот список полностью не собирается, однако согласился отвечать на вопросы следующего вида: 
Довлет называет число — порядковый номер оценки которую называл Влад, а он скажет какая оценка там была. Никита и Эдик, как сокомандники помогли 
Довлету и приготовили список из Q чисел — порядковые номера оценок, которые не расслышал Довлет. Вы же товарищ Влада, и должны помочь ему отвечать 
на вопросы Довлета: отвечайте на его запросы, то есть если Довлет запрашивает число i, вы должны вывести i-е число названное Владом.

Входные данные
В первой строке входных данных находится единственное число: N (1 ≤ N ≤ 100) — количество оценок названных Владом.

В следующей строке вводится N чисел через пробел — оценки которые назвал Влад.

В третьей строке вводится число Q - количество запросов Довлета.

В следующей строке вводится Q чисел через пробел — номера оценок которые запрашивает Довлет у Влада. (Каждая оценка представляет собой целое число 
от  - 100 до 100

Выходные данные
Вам необходимо отвечать на запросы в следующем виде: вывести Q чисел через пробел — ответы на запросы Довлета, i-е число представляет собой ответ 
i-й запрос. Рассмотрите примеры из условия и описание к ним для большего понимания задачи
*/


#include <iostream>
using namespace std;
int main() {
    int n{}; // количество оценок названных Владом
    int q{}; // количество запросов Довлета
    int masiv[100];
    int namber{};
   
    cin >> n;
    for (size_t i = 0; i < n; i++) {
        cin >> masiv[i];
    }
    cin >> q;
    for (size_t i = 0; i < q; i++) {
        cin >> namber;
        cout << masiv[namber-1] << " ";
    }
    
    return 0;
}
