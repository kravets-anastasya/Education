/*
Дан массив, состоящий из целых чисел. Назовём «горкой» элемент массива, у которого два соседа и оба меньше данного. 
Напишите программу, которая в данном массиве определит количество горок.

Входные данные
Сначала задано число N
 — количество элементов в массиве (1≤N≤100
). Далее через пробел записаны N
 чисел — элементы массива. Массив состоит из целых чисел.

Выходные данные
Необходимо вывести количество «горок» — элементов массива, у которых два соседа и которые при этом строго больше обоих своих соседей.
*/


#include <iostream>
using namespace std;
int main() {
    int n{};
    int namber{ };
    int masiv[100];
 
    cin >> n;
 
    for (size_t i = 0; i < n; i++) {
        cin >> masiv[i];
    }
    for (size_t i = 1; i < n-1; i++) {
        if (((masiv[i] > masiv[i - 1]) && (masiv[i] > masiv[i + 1]))) {
            namber++;
        }
    }
    cout << namber << endl;
 
    return 0;
}
